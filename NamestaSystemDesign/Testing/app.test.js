// const sortingAge = require("./app");

// test("testing the first user after sorting feature", () => {
//     const sortedData = sortingAge();

//     expect(sortedData[0].name).toBe('Person 2')
// })

// test("testing the last user after sorting feature", () => {
//     const sortedData = sortingAge();

//     expect(sortedData[sortedData.length - 1].name).toBe('Person 3')
// })


// test("testing if the sort data had return", () => {
//     const sortedData = sortingAge();

//     expect(sortedData).not.toBe('undefined');
//     expect(sortedData).toBeUndefined();
// })